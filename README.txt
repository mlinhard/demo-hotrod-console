HotRod console demo
-------------------

This is simple HotRod client console which can be used to put simple string values under string keys.

recognized commands:

put <key> <value>

get <key>

del <key>

exit

quit